#!/usr/bin/env ruby

ENV['RAILS_ENV'] ||= 'development'
require_relative '../config/environment'

Messaging::Messages.handle(
  queue_id: Messaging::Queue.default_id,
  handler: Handlers::Handler,
  poll: 1,
  concurrency: 5
)
