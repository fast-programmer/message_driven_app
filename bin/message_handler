#!/usr/bin/env ruby

ENV['RAILS_ENV'] ||= 'development'
require_relative '../config/environment'

require_relative '../subdomains/message_handler'

Signal.trap('INT') do
  Messages.shutdown()

  exit
end

Messages.handle(handler: MessageHandler)
